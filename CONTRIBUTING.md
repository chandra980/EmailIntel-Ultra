# Contributing

1. Add providers only when anonymous/public access is legitimate.
2. Never inflate provider counts with placeholders.
3. Add positive/negative parser tests where practical.
4. Use bounded timeouts and explicit failure states.
5. Run pytest and ruff before submitting changes.
6. Document each user-facing feature with purpose, command, output, and limitations.
